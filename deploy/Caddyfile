# Prometheus Dashboard — Caddy reverse proxy
# Replace YOUR_DOMAIN with your actual domain or use :80 for IP-only access.

YOUR_DOMAIN {
    reverse_proxy localhost:8080

    # Optional: basic auth (uncomment and set credentials)
    # basicauth {
    #     admin $2a$14$YOUR_BCRYPT_HASH
    # }
}
